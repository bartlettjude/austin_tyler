/** @type {import('next').NextConfig} */
const nextConfig = {
  // Plain <img> tags are used for portfolio photos so real client images can be
  // dropped into /public/photos with zero config. No image domains needed.
};
export default nextConfig;
